# The default selection, installed by a non-root user through sudo. This is
# what a normal VPS run looks like.
SCENARIO_USER="ubuntu"
SCENARIO_FLAGS="--yes"
SCENARIO_EXPECT="gcc git gh rg jq uv node go rustc"
