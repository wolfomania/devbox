# What `--yes` installs with no other flags: the required modules, and the
# swapfile this box is small enough to want. The point of this scenario is
# that the default selection is small; `full` covers a wide one.
SCENARIO_USER="ubuntu"
SCENARIO_FLAGS="--yes"
SCENARIO_EXPECT="gcc git"
