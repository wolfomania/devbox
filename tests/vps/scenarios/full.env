# Every module a normal box wants, added on top of the defaults. The slowest
# scenario, and the one that proves the optional modules still install.
SCENARIO_USER="ubuntu"
SCENARIO_FLAGS="--yes --with gh,cli,python,node,go,rust"
SCENARIO_EXPECT="gcc git gh rg jq uv node go rustc"
