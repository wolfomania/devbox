# The same selection installed directly as root, which is how most fresh VPS
# images are first logged into.
SCENARIO_USER="root"
# Every scenario installs into exclave; SCENARIO_ACCOUNT overrides it.
SCENARIO_FLAGS="--yes --with gh,cli,python,node,go"
SCENARIO_EXPECT="gcc git gh rg jq uv node go"
