# The defaults plus Python. Fast: no Rust, Go or Node toolchain downloads.
# Use this scenario while iterating on the harness itself.
SCENARIO_USER="ubuntu"
SCENARIO_FLAGS="--yes --with gh,cli,python"
SCENARIO_EXPECT="gcc git gh rg jq uv"
