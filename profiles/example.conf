# devbox profile
#
# One module id per line. Blank lines and lines starting with # are ignored.
# Apply with:  ./install.sh --profile profiles/example.conf --yes
# Write one with: ./install.sh --save-profile profiles/mine.conf

base
git
gh
cli
python
node
go
rust
