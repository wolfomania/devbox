# DevOps
#
# Containers, orchestration and infrastructure.

[manifest]
category = "devops"
title = "DevOps"
order = 40

[[module]]
id = "docker"
name = "Docker"
summary = "docker engine and the compose plugin"
script = "modules/optional/docker.sh"
default = false
required = false
needs_root = true
pin = "apt"
pin_kind = "apt"
size_mb = 420

[[module]]
id = "modal"
name = "Modal CLI"
summary = "client for Modal's serverless compute; requires the Python module"
script = "modules/optional/modal.sh"
requires = ["python"]
default = false
required = false
needs_root = false
pin = "1.5.5"
pin_kind = "uv"
size_mb = 60
